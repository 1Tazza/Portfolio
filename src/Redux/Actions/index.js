export const DARK_MODE = "DARK_MODE";
export const DAY_MODE = "DAY_MODE";




export const darkMode = () => {
   return { type: "DARK_MODE"}
}

export const dayMode = () => {
   return { type: "DAY_MODE"}
}